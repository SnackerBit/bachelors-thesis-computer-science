\documentclass[crop,tikz]{standalone}
\usetikzlibrary{shapes.misc}
\usepackage{amsmath}

\def\scale{1.5}
\def\gammapos{1.3}
\definecolor{customGreen}{RGB}{0, 150, 0}
\def\lineMarkingLabelOffset{0.1}

\tikzset{cross/.style={cross out, draw=black, minimum size=2*(#1-\pgflinewidth), inner sep=0pt, outer sep=0pt, thick},
cross/.default={2.5*\scale}}

\begin{document}
\begin{tikzpicture}

  % coordinate system
  \draw[-stealth, thick] (-3*\scale, 0) -- (3*\scale, 0);  
  \draw[-stealth, thick] (0, -0.5*\scale) -- (0, 3*\scale);  

  % axis labels
  \node at (3*\scale, 0.5) {$\text{Re}(\omega)$};
  \node at (0.7, 3*\scale) {$\text{Im}(\omega)$};

  % line markings
  \node [anchor=east] at (-\lineMarkingLabelOffset*\scale, \scale) {$2\pi T$};
  \node [anchor=east] at (-\lineMarkingLabelOffset*\scale, 2*\scale) {$4\pi T$};

  % crosses
  \draw (0, 0) node[cross,red] {};
  \draw (0, \scale) node[cross,red] {};
  \draw (0, 2*\scale) node[cross,red] {};

  % help lines and cross for \gamma
  \draw[dashed, thick, customGreen] (0, \gammapos*\scale) -- ++(\scale, 0);
  \draw[stealth-stealth, thick, customGreen] (1*\scale, 0) -- 
    node[right] {$\gamma$} ++(0, \gammapos*\scale);
  \draw (0, \gammapos*\scale) node[cross,red] {};

  % integral path
  \draw[-stealth, very thick, orange] (-2.5*\scale, 0) -- (-1.25*\scale, 0);
  \draw[very thick, orange] (-2.5*\scale, 0) -- (-0.6*\scale, 0) node[below] {$L_1$};
  \draw[-stealth, very thick, orange] (-1.25*\scale, 0) -- (1.25*\scale, 0);
  \draw[very thick, orange] (1.25*\scale, 0) -- (2.5*\scale, 0);
  \draw[-stealth, very thick, blue] (2.5*\scale, 0) arc [start angle=0, end angle=60, x radius=2.5*\scale, y radius=2.5*\scale];
  \draw[-stealth, very thick, blue] (2.5*\scale, 0) arc [start angle=0, end angle=120, x radius=2.5*\scale, y radius=2.5*\scale];
  \draw[very thick, blue] (2.5*\scale, 0) arc [start angle=0, end angle=70, x radius=2.5*\scale, y radius=2.5*\scale] node[above] {$L_2$};
  \draw[very thick, blue] (2.5*\scale, 0) arc [start angle=0, end angle=180, x radius=2.5*\scale, y radius=2.5*\scale];

  % radius R
  \draw[stealth-stealth, thick, customGreen] (0, 0) -- node[below left] {$R$} (-1.76776695297*\scale, 1.76776695297*\scale);


\end{tikzpicture}
\end{document}